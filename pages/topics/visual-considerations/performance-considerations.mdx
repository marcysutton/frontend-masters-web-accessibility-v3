import Callout from "../../../components/Callout";
import StackBlitz from "../../../components/StackBlitz";

# Performance as a User Experience Metric

How many clicks/how long does it take someone to figure out an interface? Can they complete a task with a keyboard/screen reader?

Nod to the classic text, Don't Make Me Think.

Simpler is usually better and easier to build & maintain.
