import Callout from "../../../components/Callout";
import StackBlitz from "../../../components/StackBlitz";

# Keyboard Trapping

What is it, when to use it, when not

Mention Escape key pattern

Show an example

(Maybe) Discuss tab trap vs. disabling background with inert/etc.
