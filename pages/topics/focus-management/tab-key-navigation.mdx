import Callout from "../../../components/Callout";
import StackBlitz from "../../../components/StackBlitz";

# Tab Key Navigation

When to use Tab vs. arrow keys - patterns and expected behavior

What is tabbable by default (jkup/focusable)

Using tabindex: Values, How to not mess it up with roles/nested el's/etc.
