import Callout from "../../../components/Callout";

# Roles, States, and Properties

Show MDN page

How to check support for a role, state, or property (e.g. aria-autocomplete)

Using ARIA in CSS selectors

Discuss states & how they do or don't affect the frontend (e.g. aria-hidden, aria-disabled, etc.)
