import Callout from "../../../components/Callout";
import StackBlitz from "../../../components/StackBlitz";

# Accessible Names and Descriptions

Aria-label, aria-labelledby, aria-describedby

Show DevTools for labeling - (it doesn't actually compute attributes left to right - spec explanation will be in advanced course)

Mention internationalization concerns

Show example of labeling: icon buttons, image within a link
