import Callout from "../../../components/Callout";
import StackBlitz from "../../../components/StackBlitz";

# HTML for an Accessible Foundation

Setting the scene for vanilla HTML/CSS/JavaScript vs. frameworks: the basics always apply

Note about frameworks: what gets rendered is the important part for users