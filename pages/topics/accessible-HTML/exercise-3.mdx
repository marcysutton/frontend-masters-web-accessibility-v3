import Callout from "../../../components/Callout";
import StackBlitz from "../../../components/StackBlitz";

# HTML Exercise

Interact and fix code in an HTML example

Form fields + labels, DIV buttons with styling, Image alt text, Link text

Prioritize on-screen link text over aria-label - Label in name etc.

Show how to verify with Chrome DevTools and VO
